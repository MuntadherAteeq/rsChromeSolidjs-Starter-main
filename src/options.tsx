import { render } from "solid-js/web";
import "./index.css";

const Options = () => {
  return (
    <>
      <div>Options Page</div>
    </>
  );
};

render(() => <Options />, document.getElementById("root") as HTMLElement);
